To run the project you need to run the code in the current folder considering the modules by this command:
```cmd
python -m main 
```

for code quality run check and autofix run:
```cmd
pylint ./
black ./
```